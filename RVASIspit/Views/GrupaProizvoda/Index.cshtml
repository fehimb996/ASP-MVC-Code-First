@model IEnumerable<RVASIspit.Models.GrupaProizvoda>

@{
    ViewBag.Title = "GrupaProizvoda Index";
}

<h2>Grupe proizvoda</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazivGrupe)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazivGrupe)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.GrupaProizvodaID }) |
                @Html.ActionLink("Details", "Details", new { id = item.GrupaProizvodaID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.GrupaProizvodaID })
            </td>
        </tr>
    }

</table>
