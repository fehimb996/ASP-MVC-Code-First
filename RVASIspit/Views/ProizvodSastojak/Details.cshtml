@model RVASIspit.Models.ProizvodSastojak

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>ProizvodSastojak</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Naziv Proizvoda
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Proizvod.Naziv)
        </dd>
        <dt>
            Naziv Sastojka
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Sastojak.NazivSastojka)
        </dd>
    </dl>
</div>

<p>
    @if (User.IsInRole("Admin"))
    {
        @Html.ActionLink("Edit", "Edit", new { proizvodId = Model.ProizvodID, sastojakId = Model.SastojakID }, new { @class = "btn btn-warning" }) <text> | </text>
    }
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
</p>
