@model RVASIspit.Models.Sastojak

@{
    ViewBag.Title = "Sastojak Details";
}

<h2>Details</h2>

<div>
    <h4>Sastojak</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NazivSastojka)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NazivSastojka)
        </dd>
    </dl>
</div>

<p>
    @if (User.IsInRole("Admin"))
    {
        @Html.ActionLink("Izmeni", "Edit", new { id = Model.SastojakID }, new { @class = "btn btn-warning" }) <text> | </text>
    }

    @Html.ActionLink("Idi nazad", "Index", null, new { @class = "btn btn-primary" })
</p>
