@model IEnumerable<RVASIspit.Models.VrstaProizvoda>

@{
    ViewBag.Title = "Vrste Proizvoda Index";
}

<h2 class="text-center">Vrste proizvoda</h2>

@if (User.IsInRole("Admin"))
{
    <div class="text-center">
        <p>
            @Html.ActionLink("Dodaj novu vrstu", "Create", null, new { @class = "btn btn-primary" })
        </p>
    </div>
}

<table class="table table-hover table-bordered table-responsive text-center align-middle">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazivVrste)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazivVrste)
            </td>
            <td>
                @if (User.IsInRole("Admin"))
                {
                    @Html.ActionLink("Edit", "Edit", new { id = item.VrstaProizvodaID }, new { @class = "btn btn-primary" })
                    <text> | </text>
                    @Html.ActionLink("Details", "Details", new { id = item.VrstaProizvodaID }, new { @class = "btn btn-primary" })
                    <text> | </text>
                    @Html.ActionLink("Delete", "Delete", new { id = item.VrstaProizvodaID }, new { @class = "btn btn-primary" })
                }
                else if (User.IsInRole("Korisnik"))
                {
                    @Html.ActionLink("Details", "Details", new { id = item.VrstaProizvodaID }, new { @class = "btn btn-primary" })
                }
            </td>
        </tr>
    }

</table>
