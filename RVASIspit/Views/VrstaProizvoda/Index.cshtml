@model IEnumerable<RVASIspit.Models.VrstaProizvoda>

@{
    ViewBag.Title = "Vrste Proizvoda Index";
}

<h2>Vrste proizvoda</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover table-bordered table-responsive text-center">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazivVrste)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazivVrste)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.VrstaProizvodaID }) |
                @Html.ActionLink("Details", "Details", new { id = item.VrstaProizvodaID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.VrstaProizvodaID })
            </td>
        </tr>
    }

</table>
